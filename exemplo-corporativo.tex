%% Arquivo de DEMONSTRAÇÃO — não é o documento de trabalho.
%% Gera uma amostra do template no modo "corporativo" (análise empresarial),
%% reaproveitando todo o conteúdo de main.tex (apenas injeta valores via \def).
%% Compile a partir da RAIZ do repositório:
%%     latexmk -pdf exemplo-corporativo.tex
%% ou rode ./gerar-exemplos.sh para gerar todos os exemplos de uma vez.
\def\TipoDoc{corporativo}
\def\Categoria{analise}
\input{main.tex}
